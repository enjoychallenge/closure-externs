/**
 * @fileoverview Externs for simple-statistics (ss):
 * https://github.com/tmcw/simple-statistics
 * @externs
 */

var ss = {};

/**
 * @param {Array.<number>} data
 * @param {number} numberOfClasses
 * @return {Array.<number>}
 */
ss.jenks = function(data, numberOfClasses) {};


